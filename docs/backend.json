{
  "entities": {
    "Transaction": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Transaction",
      "type": "object",
      "description": "Represents a single financial transaction, either an income or an expense, detailing its amount, type, date, description, and category.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Transaction entity."
        },
        "amount": {
          "type": "number",
          "description": "The monetary value of the transaction. Must be a positive number."
        },
        "type": {
          "type": "string",
          "description": "The type of transaction, indicating whether it's an income or an expense. Expected values are 'income' or 'expense'."
        },
        "description": {
          "type": "string",
          "description": "A brief description or note about the transaction."
        },
        "date": {
          "type": "string",
          "description": "The date and time when the transaction occurred.",
          "format": "date-time"
        },
        "categoryId": {
          "type": "string",
          "description": "Reference to the Category for this transaction. (Relationship: Category 1:N Transaction)"
        }
      },
      "required": [
        "id",
        "amount",
        "type",
        "description",
        "date",
        "categoryId"
      ]
    },
    "Category": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Category",
      "type": "object",
      "description": "Represents a classification for financial transactions, such as 'Groceries', 'Salary', or 'Rent'.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Category entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the category (e.g., 'Food', 'Salary', 'Transport')."
        }
      },
      "required": [
        "id",
        "name"
      ]
    },
    "Establishment": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Establishment",
      "type": "object",
      "description": "Represents a physical or virtual location where transactions occur or prices are tracked.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Establishment entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the establishment (e.g., 'Mainframe Store', 'Neo-Market')."
        },
        "type": {
          "type": "string",
          "description": "The category or type of establishment."
        },
        "createdAt": {
          "type": "string",
          "description": "The date and time when the establishment was registered.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}/categories/{categoryId}",
        "definition": {
          "entityName": "Category",
          "schema": {
            "$ref": "#/backend/entities/Category"
          },
          "description": "Stores financial categories defined by a user.",
          "params": [
            { "name": "userId" },
            { "name": "categoryId" }
          ]
        }
      },
      {
        "path": "/users/{userId}/transactions/{transactionId}",
        "definition": {
          "entityName": "Transaction",
          "schema": {
            "$ref": "#/backend/entities/Transaction"
          },
          "description": "Stores individual financial transactions for a user.",
          "params": [
            { "name": "userId" },
            { "name": "transactionId" }
          ]
        }
      },
      {
        "path": "/users/{userId}/establishments/{establishmentId}",
        "definition": {
          "entityName": "Establishment",
          "schema": {
            "$ref": "#/backend/entities/Establishment"
          },
          "description": "Stores establishments registered by a user for price comparison.",
          "params": [
            { "name": "userId" },
            { "name": "establishmentId" }
          ]
        }
      }
    ]
  }
}
